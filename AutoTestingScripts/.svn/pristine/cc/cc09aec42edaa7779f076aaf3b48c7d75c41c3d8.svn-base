//#*****************************************************************************
//# Purpose: 2方订单全额付款
//# Author:  bobby
//# Create Date: April 13, 2009
//# Modify History: 

//#*****************************************************************************

using System;
using System.Collections.Generic;
using System.Text;
using NUnit.Framework;
using WatiN.Core;
using zjsteel.appshare.App01_HomePage;
using zjsteel.appshare;
using System.Data.OracleClient;
using System.Data;

namespace zjsteel.task.T001_2ndorder
{
    [TestFixture]
    public class test1 : SignIn
    {
        
        [Test]
        public void T01_test()
        {
            /*

            //采购商登陆
            TestUserSignIn(UN2, PW2);

            browser.Link(Find.ById("ctl00_linkTrade")).Click();
            browser.WaitUntilContainsText("请在左边的菜单选择您要进行的操作。 如有疑问，请点击下面相关链接查看操作流程图或查看交易指南");
            Assert.IsTrue(browser.ContainsText("请在左边的菜单选择您要进行的操作。 如有疑问，请点击下面相关链接查看操作流程图或查看交易指南"));
            
            
            /*
            //可提资源
            ladingbillsearch fl = new ladingbillsearch();
            fl.TestUserladingbillsearch(browser, targetHost);
            
            string ladorderid = ladingbillsearch.ladorderid;
            
            //可提资源管理
            ladbillmanage fl = new ladbillmanage();
            fl.TestUserladingbillmanage(browser, targetHost);
            //查询数据库提单密码 和 ID
            string sqlStr = @"select content from 
                                  (select * from notes order by createdate desc)
                                    where rownum <=1 and target_id=:targetID";
            System.Data.OracleClient.OracleParameter [] parms ={ 
                        new OracleParameter(":targetID",1949)
                    };
            DataSet ds1 = OracleHelper.ExecuteDataset(OracleHelper.ConnectionString, CommandType.Text, sqlStr, parms);
            DataTable dt = ds1.Tables[0];
            string l1 = dt.Rows[0][0].ToString();
            
            string ladid = l1.Trim().Substring(4, 10);
            string ladpw = l1.Trim().Substring(20, 6);
            //退出功能，
            SignOut so = new SignOut();
            so.TestUserSignOut(browser);
            

            //仓库用户登陆
            TestUserSignIn(CK, CKPW);

            verifylad V1 = new verifylad();
            V1.TestUserverifylad(browser,ladid,ladpw);
            */
        } 
   

    }
}
